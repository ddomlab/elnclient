# elnclient
This package will be used to interface with the eln.

## Functions provided
### init_experiment
Used to initialize an experiment. The variables passed in are the minimal info needed to add an experiment to the ELN (title and description). This function must be run before doing anything else.
### upload_image
Will upload a png or jpg image to the experiment.

## Functions to add
- add_chemical
