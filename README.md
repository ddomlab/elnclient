# elnclient
This package will be used to interface with the eln.

## Functions provided
### init_experiment
Used to initalize an experiment. The variables passed in are the minimal info needed to add an experiment to the ELN (title and description). This function must be run before doing anything else.

## Functions to add
- upoload_image
- add_chemical
